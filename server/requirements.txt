# DEPRECATED: This file is kept as a fallback only.
# The project now uses UV for dependency management — see pyproject.toml and uv.lock.
# To install deps: cd server && uv sync
# To run: uv run python server.py
#
# Oryx (Azure App Service) picks up requirements.txt automatically.
# Keep in sync with pyproject.toml [project.dependencies].
fastmcp>=3.2
pydantic-settings>=2.0
uvicorn[standard]>=0.29
