# azure.yaml — AZD project definition for cloud-helper-fastmcp
# Created: 2026-05-09T04:22:42Z
# Migrated from scripts/provision-two-app-regs.sh to AZD + Bicep.
# Run `azd provision` to deploy infra; `azd deploy` to deploy server code.
# See infra/README.md for full setup instructions.

name: cloud-helper-fastmcp
metadata:
  template: azd-init@1.9.0

services:
  server:
    # FastMCP Python server — Naomi owns the UV/UVX packaging in server/
    project: ./server
    language: python
    host: appservice
    # AZD discovers the App Service by the azd-service-name tag set in Bicep.
    # Keep resourceName unset so each azd environment can deploy its own web app.

infra:
  provider: bicep
  path: ./infra

hooks:
  postprovision:
    shell: sh
    run: |
      echo ""
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo "🔗  Foundry MCP Connection — OAuth2 Redirect URI"
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""
      REDIRECT_URL=$(az rest --method GET \
        --url "https://management.azure.com/subscriptions/${AZURE_SUBSCRIPTION_ID}/resourceGroups/${AZURE_RESOURCE_GROUP}/providers/Microsoft.CognitiveServices/accounts/${FOUNDRY_NAME}/projects/${FOUNDRY_PROJECT_NAME}/connections/MCP-${WEB_APP_NAME}?api-version=2025-04-01-preview" \
        --query "properties.redirectUrl" -o tsv 2>/dev/null)
      if [ -n "$REDIRECT_URL" ] && [ "$REDIRECT_URL" != "None" ]; then
        echo "  ✅  redirectUrl: $REDIRECT_URL"
        echo ""
        echo "  ❗  Register this URI in the app registration:"
        echo "      Azure Portal → App registrations → ${WEB_APP_NAME}"
        echo "      → Authentication → Mobile and desktop applications"
        echo "      → Add URI: $REDIRECT_URL"
      else
        echo "  ⚠️  Could not retrieve redirectUrl (connection may still be provisioning)"
      fi
      echo ""
  postdeploy:
    shell: sh
    run: |
      echo ""
      echo "✅ Provisioning complete!"
      echo ""
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo "🔗  MCP Server Endpoints"
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""
      echo "  🟢  MCP Server (fixed OAuth): https://${FIXED_SLOT_HOSTNAME}/mcp"
      echo ""
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo "🤖  Configure Azure AI Foundry Agent — MCP Tool"
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""
      echo "  In Foundry → Your Agent → Add Tool → Model Context Protocol"
      echo "  Select: OAuth Identity Passthrough"
      echo ""
      echo "  Field                   Value"
      echo "  ─────────────────────   ──────────────────────────────────────────────────────────"
      echo "  Remote MCP endpoint   : https://${FIXED_SLOT_HOSTNAME}/mcp"
      echo "  Client ID             : ${FIXED_CLIENT_ID}"
      echo "  Client secret         : (leave blank)"
      echo "  Auth URL              : https://login.microsoftonline.com/${AZURE_TENANT_ID}/oauth2/v2.0/authorize"
      echo "  Token URL             : https://login.microsoftonline.com/${AZURE_TENANT_ID}/oauth2/v2.0/token"
      echo "  Refresh URL           : https://login.microsoftonline.com/${AZURE_TENANT_ID}/oauth2/v2.0/token"
      echo "  Scopes                : ${FIXED_AUDIENCE}/.default"
      echo ""
      echo "❗❗❗ Remember to register the Foundry redirect URI in the Azure app registration in 'Mobile and desktop applications' section for the Fixed slot (MCP Tool) to enable OAuth Identity Passthrough."
      echo ""
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo "🧪  Quick VS Code test (mcp.json snippet)"
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""
      echo '  "cloud-helper-fixed": {'
      echo '    "type": "http",'
      echo "    \"url\": \"https://${FIXED_SLOT_HOSTNAME}/mcp\""
      echo '  }'
      echo ""
